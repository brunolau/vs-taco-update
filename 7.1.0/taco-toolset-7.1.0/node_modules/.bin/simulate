#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../cordova-simulate/bin/simulate" "$@"
  ret=$?
else 
  node  "$basedir/../cordova-simulate/bin/simulate" "$@"
  ret=$?
fi
exit $ret
